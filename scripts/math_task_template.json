{
  "agent": {
    "harness": "claude_code",
    "model_name": "claude-opus-4-5",
    "skills_path": "/opt/canonical/skills",
    "settings": {
      "max_turns": 20,
      "disallowed_tools": "AskUserQuestion CronCreate CronDelete CronList EnterPlanMode EnterWorktree ExitPlanMode ExitWorktree NotebookEdit ScheduleWakeup TaskCreate TaskGet TaskList TaskOutput TaskStop TaskUpdate TodoWrite WebFetch WebSearch",
      "append_system_prompt": "You are solving a competition math problem. You MAY write and run Python (via the Bash tool) to compute or verify intermediate steps. Reason carefully, then give the final answer EXACTLY in the format requested by the problem statement (a single integer on its own line after 'Answer:'). Do not ask the user any questions in this non-interactive run."
    }
  },
  "runtime": {
    "backend": "docker",
    "image": "sandbox:v1",
    "network": "host",
    "workdir": "/opt/workspace/agent_workdir",
    "env": {
      "DISABLE_AUTOUPDATER": "1",
      "API_TIMEOUT_MS": "14400000",
      "CLAUDE_CODE_MAX_RETRIES": "1",
      "CLAUDE_CODE_MAX_OUTPUT_TOKENS": "32768",
      "POLAR_ANTHROPIC_DEFAULT_MAX_TOKENS": "32768"
    },
    "kwargs": {
      "volumes": [
        "operator_runtime:/opt/canonical:ro",
        "operator_runtime/tools:/opt/workspace/agent_workdir/tools:ro"
      ]
    }
  },
  "evaluator": {
    "strategy": "math_judge",
    "config": { "format_bonus": 0.0 }
  },
  "builder": {
    "strategy": "prefix_merging"
  }
}
