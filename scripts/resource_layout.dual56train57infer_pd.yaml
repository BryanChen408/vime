
roles:
  actor:
    - {node: 80.5.25.141, devices: "0-15"}
  rollout:
    - {node: 80.5.25.140, devices: "8-15"}
  polar_reserved:
    - {node: 80.5.25.140, devices: "4-7"}      # ← 补上
rollout:
  num_gpus_per_engine: 4                 # 8 卡 → 2 个 tp4 engine(8-11/12-15,各不跨 HCCS 域);要 tp8 改成 8
  vllm_dp_size: 2                         # external-LB DP 组大小 = 引擎数(rollout 卡数 / num_gpus_per_engine = 8/4 = 2)。
                                          #   仅 FEAT_DP_EXTERNAL_LB=1 时生效(基线忽略);须 == 引擎数,否则 arguments.py 报错。
                                          #   改 num_gpus_per_engine 时此处同步改(如 TP8 → vllm_dp_size:2)。