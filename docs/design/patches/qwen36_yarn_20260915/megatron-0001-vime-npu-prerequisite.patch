diff --git a/megatron/core/transformer/moe/moe_utils.py b/megatron/core/transformer/moe/moe_utils.py
index 5923af131..f82f22f82 100644
--- a/megatron/core/transformer/moe/moe_utils.py
+++ b/megatron/core/transformer/moe/moe_utils.py
@@ -597,7 +597,7 @@ def topk_routing_with_score_function(
         else:
             return torch.topk(scores, k=topk, dim=1)
 
-    from slime.utils.routing_replay import get_routing_replay_compute_topk
+    from vime.utils.routing_replay import get_routing_replay_compute_topk
     compute_topk = get_routing_replay_compute_topk(compute_topk)
 
     if score_function == "softmax":
diff --git a/megatron/core/transformer/moe/router.py b/megatron/core/transformer/moe/router.py
index 517944f25..d99d5c0c6 100644
--- a/megatron/core/transformer/moe/router.py
+++ b/megatron/core/transformer/moe/router.py
@@ -201,7 +201,7 @@ class TopKRouter(Router):
             self.global_tokens_per_expert = None
             self.ga_steps = None
 
-        from slime.utils.routing_replay import register_routing_replay
+        from vime.utils.routing_replay import register_routing_replay
         register_routing_replay(self)
 
     def _maintain_float32_expert_bias(self):
diff --git a/megatron/training/arguments.py b/megatron/training/arguments.py
index 83736acdc..808c1e930 100644
--- a/megatron/training/arguments.py
+++ b/megatron/training/arguments.py
@@ -903,7 +903,7 @@ def validate_args(args, defaults={}):
         assert args.fp16 or args.bf16, \
             'residual connection in fp32 only supported when using fp16 or bf16.'
 
-    if args.moe_grouped_gemm:
+    if args.moe_grouped_gemm and torch.cuda.is_available():
         dc = torch.cuda.get_device_capability()
         assert dc[0] >= 8, "Unsupported compute capability for GroupedGEMM kernels."
 
diff --git a/megatron/training/utils.py b/megatron/training/utils.py
index 4730a5252..e2e59b108 100644
--- a/megatron/training/utils.py
+++ b/megatron/training/utils.py
@@ -445,8 +445,14 @@ def is_first_or_last_pipeline_stage(vp_stage):
 
 
 def get_device_arch_version():
-    """Returns GPU arch version (8: Ampere, 9: Hopper, 10: Blackwell, ...)"""
-    return torch.cuda.get_device_properties(torch.device("cuda:0")).major
+    """Returns GPU arch version (8: Ampere, 9: Hopper, 10: Blackwell, ...).
+
+    On NPU-only builds, torch.cuda is unavailable. Fall back to 0 so callers can
+    still apply pre-Blackwell defaults without touching CUDA APIs.
+    """
+    if torch.cuda.is_available():
+        return torch.cuda.get_device_properties(torch.device("cuda:0")).major
+    return 0
 
 
 def append_to_progress_log(string, barrier=True):
diff --git a/megatron/training/yaml_arguments.py b/megatron/training/yaml_arguments.py
index 405d7b70f..0e093c764 100644
--- a/megatron/training/yaml_arguments.py
+++ b/megatron/training/yaml_arguments.py
@@ -257,8 +257,9 @@ def validate_yaml(args, defaults={}):
 
     if args.language_model.moe_grouped_gemm:
         assert args.model_parallel.bf16, 'Currently GroupedGEMM for MoE only supports bf16 dtype.'
-        dc = torch.cuda.get_device_capability()
-        assert dc[0] >= 8, "Unsupported compute capability for GroupedGEMM kernels."
+        if torch.cuda.is_available():
+            dc = torch.cuda.get_device_capability()
+            assert dc[0] >= 8, "Unsupported compute capability for GroupedGEMM kernels."
 
     if args.weight_decay_incr_style == 'constant':
         assert args.start_weight_decay is None
