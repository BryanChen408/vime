# vllm-ascend PD patch — extracted 2026-08-20
#   baseline: /home/l00619433/package_tmp/vllm-ascend-official @ 88eac2713b14beedc7a48803274f8d6cee3e9f30 (88eac2713b14)
#   fork:     /home/l00619433/package_tmp/vllm-ascend-023 @ origin/vime-adapter-v023 (a914c67a02b5)
#   generated by extract_pd_patches.sh — regenerate, do not hand-edit
diff --git a/vllm_ascend/device_allocator/camem.py b/vllm_ascend/device_allocator/camem.py
index cbc64402..c87cefd0 100644
--- a/vllm_ascend/device_allocator/camem.py
+++ b/vllm_ascend/device_allocator/camem.py
@@ -150,11 +150,17 @@ class CaMemAllocator:
 
     def __init__(self):
         conf = os.environ.get("PYTORCH_NPU_ALLOC_CONF", "")
-        assert "expandable_segments:True" not in conf, (
-            "Expandable segments are not compatible with memory pool. "
-            "Please track https://github.com/pytorch/pytorch/issues/147851 "
-            "for the latest updates."
-        )
+        # [vime 2026-07-15 EXPERIMENTAL] 硬 assert 降为 warning(用户要求,便于实验 expandable+sleep）。
+        #   官方原因保留:expandable_segments 与 CaMemAllocator 内存池不兼容(pytorch#147851),
+        #   sleep/wake(offload)可能出错。仅实验用,验完须还原本行。改此文件后必须同步到 52 并重起 run。
+        if "expandable_segments:True" in conf:
+            import logging as _logging
+
+            _logging.getLogger(__name__).warning(
+                "[vime EXPERIMENTAL] expandable_segments:True set WITH CaMemAllocator pool — "
+                "officially incompatible (pytorch#147851); sleep/wake offload may break. "
+                "Upstream hard assert was relaxed to a warning for experimentation."
+            )
 
         self.pointer_to_data: dict[int, AllocationData] = {}
         self.current_tag: str = CaMemAllocator.default_tag
diff --git a/vllm_ascend/patch/platform/__init__.py b/vllm_ascend/patch/platform/__init__.py
index 257ad017..650e0e35 100644
--- a/vllm_ascend/patch/platform/__init__.py
+++ b/vllm_ascend/patch/platform/__init__.py
@@ -53,4 +53,4 @@ import vllm_ascend.patch.platform.patch_speculative_config  # noqa
 
 if not vllm_version_is("0.23.0"):
     import vllm_ascend.patch.platform.patch_fused_moe  # noqa
-    import vllm_ascend.patch.platform.patch_dp_device_ids  # noqa
+    # import vllm_ascend.patch.platform.patch_dp_device_ids  # noqa  # [vime] Disabled for v0.23.0 - function does not exist
diff --git a/vllm_ascend/patch/platform/patch_dp_device_ids.py b/vllm_ascend/patch/platform/patch_dp_device_ids.py
index 0820451e..456bfc64 100644
--- a/vllm_ascend/patch/platform/patch_dp_device_ids.py
+++ b/vllm_ascend/patch/platform/patch_dp_device_ids.py
@@ -31,7 +31,8 @@
 
 from vllm_ascend.utils import vllm_version_is
 
-if not vllm_version_is("0.23.0"):
+# [vime fix] v0.23.0 does not have get_physical_gpu_ids_for_local_dp_rank, disable this patch
+if False:  # was: if not vllm_version_is("0.23.0"):
     import os
 
     from vllm.platforms import current_platform
