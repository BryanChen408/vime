# Python dependencies for vLLM v0.23.0 + PD-Mooncake environment

# Core dependencies
numpy>=1.23.0
torch>=2.0.0
transformers>=4.36.0
tokenizers>=0.15.0
sentencepiece>=0.1.99
ray>=2.9.0

# vLLM dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.24.0
pydantic>=2.0
requests>=2.26.0
aiohttp>=3.9.0
psutil>=5.9.0
prometheus-client>=0.18.0

# Distributed training
accelerate>=0.25.0

# Utilities
tqdm>=4.65.0
setuptools>=65.0.0
wheel>=0.40.0
packaging>=23.0

# Optional: wandb (disabled by default in environment)
# wandb>=0.15.0

# Development tools (optional)
# ipython>=8.10.0
# pytest>=7.2.0
